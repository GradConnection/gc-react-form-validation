{
  "name": "gc-react-form-validation",
  "version": "0.7.1",
  "description": "",
  "main": "lib/index.js",
  "author": "chrisnyman",
  "homepage": "https://github.com/GradConnection/gc-react-form-validation",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/GradConnection/gc-react-form-validation"
  },
  "bugs": {
    "url": "https://github.com/GradConnection/gc-react-form-validation/issues"
  },
  "license": "ISC",
  "scripts": {
    "clean": "rm -rf build",
    "build:scripts": "cd ./src && NODE_ENV=production babel -d ../lib './**/*.js'",
    "build:styles": "cp ./postcss.config.js ./src/styles.scss lib/ && NODE_ENV=production node-sass < src/styles.scss > ./lib/styles.css",
    "build": "npm run clean && npm run build:scripts && npm run build:styles",
    "start": "babel src --watch --out-dir lib"
  },
  "dependencies": {
    "lodash": "^4.17.4",
    "prop-types": "^15.5.10",
    "react": "^16.0.0",
    "react-dom": "^16.0.0",
    "react-html-parser": "^2.0.1"
  },
  "devDependencies": {
    "babel-cli": "^6.24.1",
    "babel-minify": "^0.4.3",
    "babel-plugin-uglify": "^1.0.2",
    "babel-preset-env": "^1.6.0",
    "babel-preset-es2015": "^6.24.1",
    "babel-preset-react": "^6.24.1",
    "node-sass": "^4.5.3"
  }
}
